schema {
  query: Query
  mutation: Mutation
}

type Query {
  listSites: [Site]
}

type Mutation {
  addSite(input: NewSiteInput!): Site
}

input NewSiteInput {
  key: String!,
  name: String!
}

type Site implements Node {
  id: ID!
  key: String!
  name: String!
}
