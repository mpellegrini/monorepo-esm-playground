/// <references types="houdini-svelte">

/** @type {import('houdini').ConfigFile} */
const config = {
  schemaPath: './node_modules/@packages/graphql/lib/schema.merged.graphql',
  plugins: {
    'houdini-svelte': {},
  },
}

export default config
